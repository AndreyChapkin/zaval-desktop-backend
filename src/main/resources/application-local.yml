spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/zaval_backend_test
    username: zaval_backend
    password: 1
    driver-class-name: org.postgresql.Driver
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE